# dependencies
node_modules/
**/node_modules/

# env
.env
.env.local
**/.env
!.env.example

# build output
dist/
build/
client/dist/
server/dist/

# python
ai-service/__pycache__/
ai-service/venv/
ai-service/.venv/
**/__pycache__/
*.pyc

# vector db local storage
ai-service/chroma_db/

# logs
*.log
npm-debug.log*

# editor
.vscode/
.idea/

# os
.DS_Store
Thumbs.db

# testing
coverage/
client/playwright-report/
client/test-results/
client/e2e/.auth/
# Note: client/e2e/__screenshots__/ is deliberately NOT ignored — those are the
# visual-regression baselines the suite compares against, so they belong in git.

# Local-only dev credentials. Lists a working password for every seeded test
# account, so it must never reach the remote — keep it on disk, out of git.
TEST_LOGINS.md
